#include <iostream>
#include <vector>
using namespace std;
 
int main()
{	
	int r=0, c=0;
	
	
	while(cin >> r >> c)
	{
		vector<vector<int>> resnum(c,vector<int>(r));
	
		for(int i=0;i<r;i++)
		{
			for(int j=0;j<c;j++)
			{
				cin >> resnum[j][i];
			}
		}
	
		for(int m=0;m<c;m++)
		{
			for(int n=0;n<r;n++)
			{
				cout << resnum[m][n] << " ";
			}
			cout << endl;
		}
	}
	
	
}      //哥們，* 測資檔會包含多組矩陣資料 (真的很搞...
