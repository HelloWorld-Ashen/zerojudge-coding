#include <iostream>

using namespace std;
int main()
{
    int a=0, b=0,temp=0;
    cin >> a >> b;

    if(a<b)
    {
        temp=a;
        a=b;
        b=temp;
    }

    while(b!=0)
    {
        temp=a%b;
        a=b;
        b=temp;
    }
    cout << a << "\n";

    getchar();
    getchar();
    return 0;
}
